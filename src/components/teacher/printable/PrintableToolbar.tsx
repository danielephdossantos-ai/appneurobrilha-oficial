import { BookOpen, ChevronLeft, ChevronRight, Printer } from "lucide-react";

export function PrintableToolbar({ pageNumber, total, filter, onFilter, onPrevious, onNext, onPrint }: { pageNumber: number; total: number; filter: "all" | "teacher" | "student"; onFilter: (filter: "all" | "teacher" | "student") => void; onPrevious: () => void; onNext: () => void; onPrint: (mode: "current" | "student" | "module") => void }) {
  return <div className="printable-toolbar print:hidden"><div className="toolbar-page"><BookOpen /> <b>Folha {pageNumber} de {total}</b></div><div className="toolbar-filters">{([ ["all", "Todas"], ["teacher", "Professor"], ["student", "Estudante"] ] as const).map(([value, label]) => <button key={value} className={filter === value ? "is-active" : ""} onClick={() => onFilter(value)}>{label}</button>)}</div><div className="toolbar-actions"><button title="Imprimir folha atual" onClick={() => onPrint("current")}><Printer /> Folha</button><button title="Imprimir folhas do estudante" onClick={() => onPrint("student")}><Printer /> Estudante</button><button title="Imprimir módulo completo" onClick={() => onPrint("module")}><Printer /> Módulo</button></div><div className="toolbar-navigation"><button onClick={onPrevious} aria-label="Folha anterior"><ChevronLeft /></button><button onClick={onNext} aria-label="Próxima folha"><ChevronRight /></button></div></div>;
}