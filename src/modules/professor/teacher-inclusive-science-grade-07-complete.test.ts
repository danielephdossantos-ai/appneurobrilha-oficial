import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904300000_teacher_inclusive_lessons_science_grade_07_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF07CI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("Ciências adaptada do 7º ano completa",()=>{it("cobre 16 habilidades uma vez",()=>{expect(rows).toHaveLength(16);for(let i=1;i<=16;i++)expect(rows.filter(x=>x.code===`EF07CI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/ciencias-7ano",r.rel))).toBe(true)});it("usa sistemas, evidências e progressão",()=>{for(const x of["1. Delimitar o sistema","2. Analisar evidências","3. Explicar e avaliar limites","modelos representam aspectos e têm limites","Prática guiada antes da independente"])expect(sql).toContain(x)});it("tem segurança e responsabilidade",()=>{for(const x of["não usar chama, combustíveis, eletricidade de rede","sem diagnóstico individual","prevention_only","adult_supervision","age_respectful","source_comparison"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
