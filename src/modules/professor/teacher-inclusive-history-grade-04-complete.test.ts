import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904360000_teacher_inclusive_lessons_history_grade_04_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF04HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 4º ano completa",()=>{it("cobre 11 habilidades uma vez",()=>{expect(rows).toHaveLength(11);for(let i=1;i<=11;i++)expect(rows.filter(x=>x.code===`EF04HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-4ano",r.rel))).toBe(true)});it("desenvolve investigação histórica",()=>{for(const x of["1. Situar rotas e fontes","2. Comparar causas e experiências","3. Explicar transformações","causas e consequências","Prática guiada antes da independente"])expect(sql).toContain(x)});it("preserva agência, contexto e segurança",()=>{for(const x of["não use linguagem que transforme pessoas em mercadorias","privacy_preserving","multiple_perspectives","indigenous_afro_brazilian_agency","forced_migration_context","non_graphic","no_oppression_roleplay"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","cards","map","timeline","drawing","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
