import {describe,expect,it} from "vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";
const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260902040000_teacher_inclusive_library.sql"),"utf8");
describe("biblioteca inclusiva do professor",()=>{it("separa orientação docente da aula adaptada",()=>{expect(sql).toContain("teacher_pages jsonb");expect(sql).toContain("student_pages jsonb");expect(sql).toContain("answer_key jsonb")});it("só publica material aprovado nas três verificações",()=>{expect(sql).toContain("source_fidelity_checked");expect(sql).toContain("pedagogical_reviewed");expect(sql).toContain("technical_reviewed");expect(sql.match(/l\.status='published'/g)?.length).toBeGreaterThanOrEqual(2)});it("restringe a biblioteca a professor ativo ou administrador",()=>{expect(sql.match(/TEACHER_ACCESS_REQUIRED/g)?.length).toBe(2);expect(sql).toContain("p.status='active'")});it("pesquisa por código, título, objeto e objetivo",()=>{expect(sql).toContain("l.codigo_bncc ILIKE");expect(sql).toContain("l.title ILIKE");expect(sql).toContain("l.knowledge_object");expect(sql).toContain("l.learning_goal ILIKE")})});
