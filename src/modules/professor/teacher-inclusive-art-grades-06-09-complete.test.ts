import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const migration="supabase/migrations/20260904520000_teacher_inclusive_lessons_art_grades_06_09_complete.sql";const sql=readFileSync(resolve(process.cwd(),migration),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF69AR\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("Arte inclusiva do 6º ao 9º ano completa",()=>{it("cobre 35 habilidades compartilhadas uma vez",()=>{expect(rows).toHaveLength(35);for(let i=1;i<=35;i++)expect(rows.filter(x=>x.code===`EF69AR${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes reais que declaram os códigos",()=>{for(const r of rows){const p=resolve(process.cwd(),r.source);expect(existsSync(p)).toBe(true);expect(readFileSync(p,"utf8")).toContain(r.code)}});it("diferencia progressão do 6º ao 9º",()=>{for(const x of["6º ano","7º ano","8º ano","9º ano","1. Pesquisar e analisar","2. Experimentar e desenvolver","3. Apresentar e avaliar"])expect(sql).toContain(x)});it("garante consentimento, segurança e privacidade",()=>{for(const x of["safe_nontoxic_materials","no_student_access_to_blades_flame_solvents_aerosols_hot_glue","allergy_alternative","no_forced_touch","no_forced_performance","no_forced_recording","privacy_preserving","volume_control"])expect(sql).toContain(x)});it("protege cultura, história, autoria e inclusão",()=>{for(const x of["body_dignity","cultural_context_required","sacred_practice_respect","no_identity_impersonation","historical_non_graphic","no_immersive_violence_dramatization","copyright_respect","license_and_credit","mobility_vision_hearing_inclusive","backstage_role","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
