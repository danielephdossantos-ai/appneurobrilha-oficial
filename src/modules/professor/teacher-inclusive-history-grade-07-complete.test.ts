import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904390000_teacher_inclusive_lessons_history_grade_07_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF07HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 7º ano completa",()=>{it("cobre 17 habilidades uma vez",()=>{expect(rows).toHaveLength(17);for(let i=1;i<=17;i++)expect(rows.filter(x=>x.code===`EF07HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-7ano",r.rel))).toBe(true)});it("desenvolve análise histórica",()=>{for(const x of["1. Situar fontes e conceitos","2. Relacionar redes, interesses e coerções","3. Argumentar com múltiplas perspectivas","silêncios e limites","Prática guiada antes da independente"])expect(sql).toContain(x)});it("preserva agência, contexto e segurança",()=>{for(const x of["Problematize termos como descobrimento","privacy_preserving","multiple_perspectives","indigenous_african_quilombola_agency","contested_terms","forced_labor_context","non_graphic","no_oppression_roleplay"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","map","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
