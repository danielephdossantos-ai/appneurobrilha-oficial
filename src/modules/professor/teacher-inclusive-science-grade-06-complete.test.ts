import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904270000_teacher_inclusive_lessons_science_grade_06_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF06CI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("Ciências adaptada do 6º ano completa",()=>{it("cobre 14 habilidades uma vez",()=>{expect(rows).toHaveLength(14);for(let i=1;i<=14;i++)expect(rows.filter(x=>x.code===`EF06CI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/ciencias-6ano",r.rel))).toBe(true)});it("usa sistemas, modelos e progressão",()=>{for(const x of["1. Delimitar o sistema","2. Analisar evidências","3. Explicar e avaliar limites","modelos representam aspectos e têm limites"])expect(sql).toContain(x)});it("tem segurança e prevenção",()=>{for(const x of["nunca demonstrar, normalizar ou instruir acesso","prevention_only","adult_supervision","age_respectful"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});

