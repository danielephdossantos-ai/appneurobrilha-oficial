import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904330000_teacher_inclusive_lessons_history_grade_01_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF01HI\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("História adaptada do 1º ano completa",()=>{it("cobre 8 habilidades uma vez",()=>{expect(rows).toHaveLength(8);for(let i=1;i<=8;i++)expect(rows.filter(x=>x.code===`EF01HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),r.source))).toBe(true)});it("desenvolve pensamento histórico",()=>{for(const x of["1. Observar a fonte","2. Ordenar e comparar","3. Contar com evidência","antes, agora e depois","mudança e permanência","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege diversidade e privacidade",()=>{for(const x of["nunca exija fotografia, documento, endereço, religião ou relato familiar","privacy_preserving","family_diversity","cultural_religious_respect","fictional_alternative"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","cards","objects","drawing","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
