import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const migration="supabase/migrations/20260904540000_teacher_inclusive_lessons_english_grade_06_complete.sql";const sql=readFileSync(resolve(process.cwd(),migration),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF06LI\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("Inglês inclusivo do 6º ano completo",()=>{it("cobre 26 habilidades uma vez",()=>{expect(rows).toHaveLength(26);for(let i=1;i<=26;i++)expect(rows.filter(x=>x.code===`EF06LI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes reais que declaram cada código",()=>{for(const r of rows){const p=resolve(process.cwd(),r.source);expect(existsSync(p)).toBe(true);expect(readFileSync(p,"utf8")).toContain(r.code)}});it("ensina compreensão e produção estratégicas",()=>{for(const x of["1. Antecipar e compreender","2. Praticar com apoio","3. Comunicar e revisar","skimming","scanning","propósito, público, ideias"])expect(sql).toContain(x)});it("garante acessibilidade linguística",()=>{for(const x of["captions","transcript","audio_replay","playback_speed_control","no_forced_speaking","no_forced_read_aloud","accent_dignity","multilingual_respect","aac","assistive_technology"])expect(sql).toContain(x)});it("protege dados, cultura e autoria",()=>{for(const x of["privacy_preserving","fictional_personal_context","no_live_geolocation","no_personal_account_required","no_public_posting","no_family_disclosure","no_health_disclosure","cultural_respect","copyright_respect"])expect(sql).toContain(x)})});
