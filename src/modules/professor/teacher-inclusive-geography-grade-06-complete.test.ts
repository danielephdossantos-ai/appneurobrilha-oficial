import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const migration="supabase/migrations/20260904470000_teacher_inclusive_lessons_geography_grade_06_complete.sql";const sql=readFileSync(resolve(process.cwd(),migration),"utf8");const source="src/escola-brilha/curso-v4/geografia-6ano/dados-fund2.ts";const rows=Array.from(sql.matchAll(/\('(?<code>EF06GE\d{2})','(?<title>[^']+)'/g),m=>m.groups!);describe("Geografia adaptada do 6º ano completa",()=>{it("cobre 13 habilidades uma vez",()=>{expect(rows).toHaveLength(13);for(let i=1;i<=13;i++)expect(rows.filter(x=>x.code===`EF06GE${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa banco real que declara todas as habilidades",()=>{const p=resolve(process.cwd(),source);expect(existsSync(p)).toBe(true);const content=readFileSync(p,"utf8");for(const r of rows)expect(content).toContain(r.code)});it("ensina investigação científica-geográfica",()=>{for(const x of["1. Formular e prever","2. Analisar evidências","3. Concluir e revisar","correlação de causalidade","revise a hipótese"])expect(sql).toContain(x)});it("protege dados e acessibilidade",()=>{for(const x of["nunca solicitar endereço, geolocalização","privacy_preserving","aggregated_public_data_only","no_live_geolocation","no_household_consumption_disclosure","no_family_workplace_collection","indigenous_community_rights_context","disaster_risk_contextualized","color_independent_symbols","accessible_charts","tactile_option"])expect(sql).toContain(x)});it("tem respostas alternativas",()=>{for(const x of["oral","pointing","cards","map","table","chart","model","diagram","drawing","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
