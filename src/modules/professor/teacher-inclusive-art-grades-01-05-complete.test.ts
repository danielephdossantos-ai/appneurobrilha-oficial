import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const migration="supabase/migrations/20260904510000_teacher_inclusive_lessons_art_grades_01_05_complete.sql";const sql=readFileSync(resolve(process.cwd(),migration),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF15AR\d{2})','(?<title>[^']+)'/g),m=>m.groups!);describe("Arte inclusiva do 1º ao 5º ano completa",()=>{it("cobre 26 habilidades compartilhadas uma vez",()=>{expect(rows).toHaveLength(26);for(let i=1;i<=26;i++)expect(rows.filter(x=>x.code===`EF15AR${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa 26 fontes reais que declaram os códigos",()=>{for(const r of rows){const p=resolve(process.cwd(),`src/escola-brilha/data/${r.code}.ts`);expect(existsSync(p)).toBe(true);expect(readFileSync(p,"utf8")).toContain(r.code)}});it("diferencia progressão do 1º ao 5º",()=>{for(const x of["1º–2º anos","3º–4º anos","5º ano","1. Apreciar e perceber","2. Experimentar e criar","3. Compartilhar e refletir"])expect(sql).toContain(x)});it("garante consentimento, segurança e privacidade",()=>{for(const x of["safe_nontoxic_materials","no_student_access_to_blades_flame_solvents_hot_glue","allergy_alternative","no_forced_touch","no_forced_performance","no_forced_recording","privacy_preserving","volume_control"])expect(sql).toContain(x)});it("protege cultura, autoria e inclusão",()=>{for(const x of["body_dignity","cultural_context_required","sacred_practice_respect","anti_stereotype","copyright_respect","mobility_vision_hearing_inclusive","backstage_role","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
