import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904250000_teacher_inclusive_lessons_science_grade_04_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF04CI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("Ciências adaptada do 4º ano completa",()=>{it("cobre EF04CI01 a EF04CI11 uma vez",()=>{expect(rows).toHaveLength(11);for(let i=1;i<=11;i++)expect(rows.filter(x=>x.code===`EF04CI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/ciencias-4ano",r.rel))).toBe(true)});it("tem investigação e progressão",()=>{for(const x of["1. Prever e modelar","2. Comparar evidências","3. Explicar e transferir","variável, observação, dado e conclusão"])expect(sql).toContain(x)});it("tem segurança",()=>{for(const x of["não manipula calor","não substitua orientação profissional","safety_screened","adult_supervision"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","table","drawing","photo","typing","writing","grayscale-safe"])expect(sql).toContain(x)})});

