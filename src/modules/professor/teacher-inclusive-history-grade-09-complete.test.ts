import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904410000_teacher_inclusive_lessons_history_grade_09_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF09HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 9º ano completa",()=>{it("cobre 36 habilidades uma vez",()=>{expect(rows).toHaveLength(36);for(let i=1;i<=36;i++)expect(rows.filter(x=>x.code===`EF09HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-9ano",r.rel))).toBe(true)});it("desenvolve análise crítica",()=>{for(const x of["1. Situar e verificar fontes","2. Explicar processos e disputas","3. Argumentar e revisar","fato documentado, interpretação historiográfica, memória, opinião, propaganda e desinformação","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege dignidade, segurança e privacidade",()=>{for(const x of["não exiba imagens chocantes","privacy_preserving","source_verification","anti_disinformation","marginalized_groups_agency","memory_and_justice","non_graphic","no_violence_roleplay","no_shocking_media"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","map","diagram","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
