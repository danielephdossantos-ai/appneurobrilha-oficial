import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const expected=[11,11,11,11,12,13,12,27,18];describe("Geografia inclusiva do 1º ao 9º ano completa",()=>{it("mantém todas as 126 habilidades sem repetição por ano",()=>{let total=0;for(let grade=1;grade<=9;grade++){const stamp=String(41+grade).padStart(2,"0");const file=`supabase/migrations/20260904${stamp}0000_teacher_inclusive_lessons_geography_grade_${String(grade).padStart(2,"0")}_complete.sql`;const sql=readFileSync(resolve(process.cwd(),file),"utf8");const prefix=`EF${String(grade).padStart(2,"0")}GE`;const codes=Array.from(sql.matchAll(new RegExp(`\\('${prefix}\\d{2}'`,"g")),m=>m[0].slice(2,-1));expect(codes).toHaveLength(expected[grade-1]);expect(new Set(codes).size).toBe(codes.length);total+=codes.length}expect(total).toBe(126)})});
