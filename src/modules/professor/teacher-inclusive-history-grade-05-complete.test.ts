import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904370000_teacher_inclusive_lessons_history_grade_05_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF05HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 5º ano completa",()=>{it("cobre 10 habilidades uma vez",()=>{expect(rows).toHaveLength(10);for(let i=1;i<=10;i++)expect(rows.filter(x=>x.code===`EF05HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-5ano",r.rel))).toBe(true)});it("desenvolve investigação e argumentação",()=>{for(const x of["1. Contextualizar e ler fontes","2. Comparar conceitos e perspectivas","3. Argumentar com evidências","fato verificável, interpretação, opinião e desinformação","Prática guiada antes da independente"])expect(sql).toContain(x)});it("preserva agência, diversidade e privacidade",()=>{for(const x of["privacy_preserving","multiple_perspectives","indigenous_afro_brazilian_agency","disability_agency","source_verification","contested_memory","non_graphic"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","cards","timeline","drawing","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
