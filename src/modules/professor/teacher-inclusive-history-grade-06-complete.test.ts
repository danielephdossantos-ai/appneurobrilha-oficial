import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904380000_teacher_inclusive_lessons_history_grade_06_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF06HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 6º ano completa",()=>{it("cobre 19 habilidades uma vez",()=>{expect(rows).toHaveLength(19);for(let i=1;i<=19;i++)expect(rows.filter(x=>x.code===`EF06HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-6ano",r.rel))).toBe(true)});it("desenvolve análise histórica",()=>{for(const x of["1. Situar problema e fontes","2. Relacionar processos","3. Argumentar historicamente","silêncios e limites","Prática guiada antes da independente"])expect(sql).toContain(x)});it("preserva pluralidade, agência e segurança",()=>{for(const x of["periodizações como escolhas","privacy_preserving","multiple_perspectives","indigenous_african_agency","periodization_plurality","forced_labor_context","non_graphic","no_oppression_roleplay"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","map","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
