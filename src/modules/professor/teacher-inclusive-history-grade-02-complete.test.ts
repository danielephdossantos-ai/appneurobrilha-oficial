import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904340000_teacher_inclusive_lessons_history_grade_02_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF02HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 2º ano completa",()=>{it("cobre 11 habilidades uma vez",()=>{expect(rows).toHaveLength(11);for(let i=1;i<=11;i++)expect(rows.filter(x=>x.code===`EF02HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-2ano",r.rel))).toBe(true)});it("desenvolve pensamento histórico",()=>{for(const x of["1. Ler a fonte","2. Ordenar e comparar","3. Explicar com evidência","passado, presente e futuro","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege diversidade e privacidade",()=>{for(const x of["nunca exija fotografia, certidão, endereço, religião, ocupação ou relato familiar","rede de pessoas importantes","privacy_preserving","family_diversity","fictional_alternative","multiple_perspectives","non_stigmatizing_language"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","cards","objects","drawing","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
