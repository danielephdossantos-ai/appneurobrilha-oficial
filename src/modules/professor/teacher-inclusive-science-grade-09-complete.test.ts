import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904320000_teacher_inclusive_lessons_science_grade_09_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF09CI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("Ciências adaptada do 9º ano completa",()=>{it("cobre 17 habilidades uma vez",()=>{expect(rows).toHaveLength(17);for(let i=1;i<=17;i++)expect(rows.filter(x=>x.code===`EF09CI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/ciencias-9ano",r.rel))).toBe(true)});it("usa modelos, cálculos e argumentação",()=>{for(const x of["1. Delimitar o problema e o modelo","2. Analisar dados e explicações","3. Argumentar e avaliar limites","Prática guiada antes da independente","incertezas, implicações e limites"])expect(sql).toContain(x)});it("protege segurança, idade e privacidade",()=>{for(const x of["não usar rede elétrica, laser, fontes de radiação","não fazer diagnóstico, previsão individual","casos fictícios","privacy_preserving","non_deterministic_language","adult_supervision","age_respectful"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
