import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904420000_teacher_inclusive_lessons_geography_grade_01_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF01GE\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("Geografia adaptada do 1º ano completa",()=>{it("cobre 11 habilidades uma vez",()=>{expect(rows).toHaveLength(11);for(let i=1;i<=11;i++)expect(rows.filter(x=>x.code===`EF01GE${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),r.source))).toBe(true)});it("desenvolve pensamento espacial",()=>{for(const x of["1. Observar o lugar","2. Localizar e comparar","3. Explicar ou representar","símbolo e legenda","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege localização, diversidade e acessibilidade",()=>{for(const x of["nunca peça endereço, geolocalização","privacy_preserving","fictional_location_only","no_live_geolocation","housing_dignity","mobility_vision_inclusive"])expect(sql).toContain(x)});it("tem respostas alternativas",()=>{for(const x of["oral","pointing","cards","objects","model","drawing","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
