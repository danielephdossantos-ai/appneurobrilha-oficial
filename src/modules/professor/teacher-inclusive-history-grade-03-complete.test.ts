import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904350000_teacher_inclusive_lessons_history_grade_03_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF03HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 3º ano completa",()=>{it("cobre 12 habilidades uma vez",()=>{expect(rows).toHaveLength(12);for(let i=1;i<=12;i++)expect(rows.filter(x=>x.code===`EF03HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-3ano",r.rel))).toBe(true)});it("desenvolve investigação histórica",()=>{for(const x of["1. Situar e ler fontes","2. Comparar mudanças e perspectivas","3. Explicar com evidências","silêncios e limites","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege grupos, memória e privacidade",()=>{for(const x of["não os reduza a uma mistura homogênea","privacy_preserving","multiple_perspectives","anti_stereotype","indigenous_afro_brazilian_agency","contested_memory","non_stigmatizing_language"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","cards","map","drawing","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
