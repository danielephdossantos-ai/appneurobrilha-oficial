import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904260000_teacher_inclusive_lessons_science_grade_05_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF05CI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("Ciências adaptada do 5º ano completa",()=>{it("cobre 13 habilidades uma vez",()=>{expect(rows).toHaveLength(13);for(let i=1;i<=13;i++)expect(rows.filter(x=>x.code===`EF05CI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/ciencias-5ano",r.rel))).toBe(true)});it("usa modelos e dados",()=>{for(const x of["1. Prever e representar","2. Analisar dados","3. Explicar e aplicar","observação de inferência"])expect(sql).toContain(x)});it("protege saúde e segurança",()=>{for(const x of["nunca para o Sol","não pesar, comparar corpos","body_neutral","adult_supervision"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","diagram","drawing","photo","typing","writing","grayscale-safe"])expect(sql).toContain(x)})});

