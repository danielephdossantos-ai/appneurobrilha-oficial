import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904430000_teacher_inclusive_lessons_geography_grade_02_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF02GE\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("Geografia adaptada do 2º ano completa",()=>{it("cobre 11 habilidades uma vez",()=>{expect(rows).toHaveLength(11);for(let i=1;i<=11;i++)expect(rows.filter(x=>x.code===`EF02GE${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes que declaram a habilidade",()=>{for(const r of rows){const p=resolve(process.cwd(),r.source);expect(existsSync(p)).toBe(true);expect(readFileSync(p,"utf8")).toContain(r.code)}});it("desenvolve pensamento geográfico",()=>{for(const x of["1. Observar e identificar","2. Localizar, ordenar ou comparar","3. Relacionar e explicar","Diferencie fato observado de hipótese","Prática guiada antes da independente"])expect(sql).toContain(x)});it("protege privacidade, diversidade e acessibilidade",()=>{for(const x of["nunca peça endereço, geolocalização","privacy_preserving","fictional_location_only","no_live_geolocation","no_personal_migration_disclosure","housing_dignity","cultural_respect","mobility_vision_inclusive"])expect(sql).toContain(x)});it("tem respostas alternativas",()=>{for(const x of["oral","pointing","cards","objects","model","drawing","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
