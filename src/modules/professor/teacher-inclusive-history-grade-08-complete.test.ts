import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904400000_teacher_inclusive_lessons_history_grade_08_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF08HI\d{2})','(?<rel>[^']+\.ts)','(?<title>[^']+)'\)/g),m=>m.groups!);describe("História adaptada do 8º ano completa",()=>{it("cobre 27 habilidades uma vez",()=>{expect(rows).toHaveLength(27);for(let i=1;i<=27;i++)expect(rows.filter(x=>x.code===`EF08HI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes",()=>{for(const r of rows)expect(existsSync(resolve(process.cwd(),"src/escola-brilha/curso-v4/historia-8ano",r.rel))).toBe(true)});it("desenvolve análise e revisão",()=>{for(const x of["1. Situar fontes e conceitos","2. Explicar processos e disputas","3. Argumentar e revisar interpretações","silêncios e limites","Prática guiada antes da independente"])expect(sql).toContain(x)});it("preserva agência, contexto e segurança",()=>{for(const x of["evitando narrativas de herói único","privacy_preserving","multiple_perspectives","indigenous_afro_descendant_agency","women_workers_agency","contested_interpretations","forced_labor_context","non_graphic","no_violence_roleplay"])expect(sql).toContain(x)});it("tem acessibilidade",()=>{for(const x of["oral","pointing","map","diagram","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
