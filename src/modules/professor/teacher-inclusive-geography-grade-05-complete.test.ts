import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260904460000_teacher_inclusive_lessons_geography_grade_05_complete.sql"),"utf8");const rows=Array.from(sql.matchAll(/\('(?<code>EF05GE\d{2})','(?<source>[^']+\.ts)','(?<title>[^']+)'/g),m=>m.groups!);describe("Geografia adaptada do 5º ano completa",()=>{it("cobre 12 habilidades uma vez",()=>{expect(rows).toHaveLength(12);for(let i=1;i<=12;i++)expect(rows.filter(x=>x.code===`EF05GE${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fontes existentes que declaram a habilidade",()=>{for(const r of rows){const p=resolve(process.cwd(),r.source);expect(existsSync(p)).toBe(true);expect(readFileSync(p,"utf8")).toContain(r.code)}});it("ensina análise crítica",()=>{for(const x of["1. Verificar a fonte","2. Identificar padrões e relações","3. Avaliar e argumentar","evidência, inferência e opinião","efeitos não previstos"])expect(sql).toContain(x)});it("protege dados e acessibilidade",()=>{for(const x of["nunca peça endereço, geolocalização, renda","privacy_preserving","aggregated_public_data_only","no_live_geolocation","no_income_collection","no_household_consumption_disclosure","no_family_origin_disclosure","no_parent_occupation_collection","inequality_contextualized","color_independent_symbols","accessible_charts"])expect(sql).toContain(x)});it("tem respostas alternativas",()=>{for(const x of["oral","pointing","cards","map","table","chart","diagram","drawing","audio","typing","writing","assistive_technology","grayscale-safe"])expect(sql).toContain(x)})});
