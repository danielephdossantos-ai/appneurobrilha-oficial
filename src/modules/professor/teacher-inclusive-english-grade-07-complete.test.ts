import{describe,expect,it}from"vitest";import{existsSync,readFileSync}from"node:fs";import{resolve}from"node:path";const migration="supabase/migrations/20260904550000_teacher_inclusive_lessons_english_grade_07_complete.sql";const sql=readFileSync(resolve(process.cwd(),migration),"utf8");const source="src/escola-brilha/ingles-7ano/bncc-workshops.ts";const rows=Array.from(sql.matchAll(/\('(?<code>EF07LI\d{2})','(?<title>[^']+)'/g),m=>m.groups!);describe("Inglês inclusivo do 7º ano completo",()=>{it("cobre 23 habilidades uma vez",()=>{expect(rows).toHaveLength(23);for(let i=1;i<=23;i++)expect(rows.filter(x=>x.code===`EF07LI${String(i).padStart(2,"0")}`)).toHaveLength(1)});it("usa fonte real que declara todas as habilidades",()=>{const p=resolve(process.cwd(),source);expect(existsSync(p)).toBe(true);const content=readFileSync(p,"utf8");for(const r of rows)expect(content).toContain(r.code)});it("ensina leitura, língua e produção",()=>{for(const x of["1. Prever e compreender","2. Relacionar e praticar","3. Produzir e revisar","relações entre parágrafos","verificação de autoria"])expect(sql).toContain(x)});it("garante acessibilidade linguística",()=>{for(const x of["captions","transcript","audio_replay","playback_speed_control","no_forced_speaking","no_forced_read_aloud","no_peer_interview_required","accent_dignity","multilingual_respect","aac","assistive_technology"])expect(sql).toContain(x)});it("protege dados e pesquisa",()=>{for(const x of["privacy_preserving","fictional_biography_only","no_live_geolocation","no_personal_account_required","no_contact_with_strangers","no_public_posting","no_life_story_disclosure","no_health_disclosure","no_religion_ethnicity_nationality_collection","source_verification","copyright_respect"])expect(sql).toContain(x)})});
