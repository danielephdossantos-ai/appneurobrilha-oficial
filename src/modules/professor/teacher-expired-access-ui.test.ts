import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const ui=readFileSync(resolve(process.cwd(),"src/routes/area-professor.tsx"),"utf8");describe("bloqueio visual de acesso docente inativo",()=>{it("confere estado e validade",()=>{expect(ui).toContain('profile.status==="active"');expect(ui).toContain("new Date(profile.access_expires_at).getTime()>Date.now()")});it("não exibe o painel para conta vencida ou suspensa",()=>{expect(ui).toContain("if(!accessActive)return");expect(ui).toContain("Acesso docente inativo")});it("permite reativação por novo código",()=>{expect(ui).toContain("Novo código docente");expect(ui).toContain("Reativar acesso");expect(ui).toContain("onClick={activate}")})});
